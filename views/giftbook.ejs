<div>
<!-- Greeting hidden for cleaner UI -->  <% if (recipients.length === 0) { %>
    <p>You haven't added anyone yet.</p>
  <% } else { %>
    <ul>
  <% recipients.forEach(r => { %>
    <li>
      <strong><%= r.first_name %> <%= r.last_name %></strong>
      — <%= r.occasion %>
      (<%= r.important_date ? r.important_date.toDateString() : 'No date' %>)
    </li>
  <% }) %>
</ul>
  <% } %>
</div>
